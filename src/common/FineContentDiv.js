import React from 'react';

const FineContentDiv = (props) => {
    
    return <div style={{paddingLeft: `30px`, paddingRight: `30px`, paddingTop: `10px`}}>{props.children}</div>
    
}

export default FineContentDiv;